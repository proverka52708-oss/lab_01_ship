# Lab 01: The Event Loop Is the Game Loop

Навчальний проєкт з курсу **«JavaScript: побудова мультиплеєрної браузерної гри»**.

## Про проєкт

У цій лабораторній роботі досліджується, як JavaScript виконується в браузері, і як на основі цього побудувати ігровий цикл. Проєкт реалізує просту гру з керованим космічним кораблем на Canvas 2D.

Корабель рухається по арені за допомогою клавіатури. Симуляція працює з фіксованим кроком, а відображення синхронізоване з частотою оновлення екрана через `requestAnimationFrame`.

## Що вивчається

call stack, heap і принцип run-to-completion;
task queue та microtask queue;
порядок виконання синхронного коду, Promise і таймерів;
робота `requestAnimationFrame` у циклі браузера;
ES-модулі, `import` і `export`;
блочна область видимості `let` і `const`;
замикання для збереження стану клавіатури;
Canvas 2D API та трансформації `translate` і `rotate`;
фіксований timestep і інтерполяція стану між кадрами;
вплив блокування головного потоку на плавність гри.

## Запуск

Потрібні Node.js і npm.

```bash
npm install
npm run dev
```

Після запуску Vite відкрийте адресу, яку покаже термінал, зазвичай:

```text
http://localhost:5173
```

Для перевірки production-збірки:

```bash
npm run build
npm run preview
```

## Керування

| Клавіша              | Дія                      |
| -------------------- | ------------------------ |
| `ArrowUp` або `W`    | прискорення корабля      |
| `ArrowLeft` або `A`  | поворот ліворуч          |
| `ArrowRight` або `D` | поворот праворуч         |
| `R`                  | скидання позиції корабля |

## Архітектура

Ігровий цикл побудований за схемою фіксованого timestep:

```js
const step = 1 / 60
accumulator += Math.min(deltaTime, 0.25)

while (accumulator >= step) {
  simulate(step)
  accumulator -= step
}

render(accumulator / step)
```

Це розділяє симуляцію та рендеринг:

- симуляція виконується рівно 60 разів на секунду незалежно від частоти монітора;
- рендеринг виконується через `requestAnimationFrame`;
- `alpha` використовується для плавної інтерполяції між попереднім і поточним станом;
- величина frame delta обмежується, щоб після довгої паузи не виникала «спіраль смерті».

Стан клавіатури зберігається в замиканні `createInput()`. Функція фізики корабля не залежить від DOM або Canvas і працює лише зі станом корабля, введенням і `dt`.

Canvas масштабується відповідно до `devicePixelRatio`, тому зображення залишається чітким на екранах із високою щільністю пікселів. Коли корабель виходить за межі арени, він з'являється з протилежного боку.

## Структура проєкту

```text
my-game/
├── index.html
├── package.json
├── public/
└── src/
    ├── main.js
    ├── input.js
    ├── loop.js
    ├── style.css
    ├── sim/
    │   ├── arena.js
    │   └── ship.js
    └── render/
        ├── canvas.js
        └── draw.js
```

## Результати вимірювань

Під час звичайного запуску на дисплеї з частотою 60 Hz HUD показує приблизно:

| Показник           |         Результат |
| ------------------ | ----------------: |
| Simulation steps/s |                60 |
| Render frames/s    |      приблизно 60 |
| Frame time         | приблизно 16.7 ms |

На дисплеї з частотою 120 Hz кількість кадрів рендерингу зростає приблизно до 120 fps, але симуляція залишається на рівні 60 steps/s.

## Експерименти

### 1. Блокування головного потоку

У рендер додано навмисне очікування на 100 ms кожен 60-й кадр:

```js
const end = performance.now() + 100
while (performance.now() < end) {}
```

У результаті на екрані з'являється помітний ривок, frame time тимчасово зростає приблизно до 100 ms, а frames/s зменшується. JavaScript не може виконувати інший код сторінки паралельно на тому самому головному потоці. Поки синхронний цикл не завершиться, браузер не обробить введення, не виконає наступний callback і не намалює новий кадр.

Після видалення busy-wait анімація знову стає плавною.

### 2. `setInterval` замість `requestAnimationFrame`

`requestAnimationFrame` замінено на `setInterval(frame, 16)`.

Спостереження:

- таймер не синхронізований із моментом перемальовування екрана;
- frame time має більший jitter;
- значення 16 ms не відповідає точному інтервалу 60 Hz, який становить приблизно 16.67 ms;
- у фоновій вкладці таймери браузер обмежує, тому анімація сповільнюється або майже зупиняється;
- після повернення у вкладку з'являються нерівномірні інтервали кадрів.

Після повернення до `requestAnimationFrame` рендеринг знову прив'язаний до циклу оновлення дисплея.

### 3. Змінний timestep замість фіксованого

В accumulator-циклі виконання симуляції замінено на один виклик `simulate(dt)` для кожного кадру.

На навантаженому процесорі кадри приходять рідше, тому `dt` стає більшим і корабель рухається іншою траєкторією. Отже, результат залежить від частоти кадрів і продуктивності комп'ютера.

Після повернення до фіксованого кроку `1 / 60` симуляція виконує однакову кількість кроків за однаковий проміжок часу. Це робить поведінку детермінованою та важливою основою для майбутньої мережевої синхронізації у мультиплеєрі.

## Висновки

У лабораторній роботі показано, що JavaScript виконує код на одному головному потоці, тому довгі синхронні операції безпосередньо впливають на інтерфейс і гру. `requestAnimationFrame` відповідає за своєчасний рендеринг, а fixed timestep відокремлює фізику від частоти оновлення дисплея.

Окремі модулі, замикання для введення, чиста функція фізики та інтерполяція стану створюють основу, яку можна розширити до повноцінної мультиплеєрної гри в наступних лабораторних роботах.

## Технології

- JavaScript ES Modules;
- Vite;
- Canvas 2D API;
- `requestAnimationFrame`;
- fixed timestep game loop;
- HTML5 і CSS3.

## Автор

Шевченко Максим Віталійович 
